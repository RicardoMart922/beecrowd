#include <stdio.h>

int verificacao(int a1, int l1, int c1, int a2, int l2, int c2) {
    // 1, se apenas o primeiro bloco pode ser empilhado sobre o segundo.
    //if ( < )
}

int main() {
    int n;

    scanf("%d", &n);

    while (n--) {
        int altura1, largura1, comprimento1;
        int altura2, largura2, comprimento2;

        scanf("%d %d %d", &altura1, &largura1, &comprimento1);
        scanf("%d %d %d", &altura2, &largura2, &comprimento2);

        //int ans = verificacao();

        //printf("%d\n", ans);
    }

    return 0;
}